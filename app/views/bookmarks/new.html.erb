<h1><%= @list.name %> Playlist new bookmark</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-success" %>
  <%= link_to 'Back to the list', list_path(@list), class: " btn btn-warning" %>
<% end %>
